# Kebbi 觸摸問答 — 除錯筆記 / 踩坑紀錄

這份文件記錄從架設 Mosquitto/ngrok，到把 NUWA Roflow 跟 quiz.html 串起來過程中，實際踩過的坑跟最後找到的解法。給之後接手的人（包含未來的自己）當參考，不用重新繞一次。

---

## 一、整體架構

```
凱比機器人（NUWA Roflow 服務流程）
    ↕ MQTT
Mosquitto Broker（跑在 Mac 本機，1883 原生 + 9001 WebSocket）
    ↕ MQTT over WebSocket
quiz.html（瀏覽器，另一台螢幕/平板顯示，不一定要在凱比自己的螢幕上）
```

**關鍵原則**：凱比只負責「感測觸摸 → 發送 MQTT」跟「收到 MQTT → 做動作」，所有遊戲邏輯（出題、判斷對錯、計分）都在 `quiz.html` 這個純前端網頁裡完成。

### MQTT Topic 約定

| Topic | 方向 | Payload |
|---|---|---|
| `kebbi/touch` | 凱比 → 網頁 | `head` / `chest` / `left_hand` / `right_hand` |
| `kebbi/action` | 網頁 → 凱比 | `clap`（答對）/ `wrong`（答錯） |

### 關鍵檔案

| 檔案 | 用途 |
|---|---|
| `pulau-bahasa-jawa/quiz.html` | 遊戲主體網頁 |
| `pulau-bahasa-jawa/ngrok-url.json` | 網頁開啟時自動帶入連線網址用（見下方腳本） |
| `pulau-bahasa-jawa/update-ngrok-url.sh` | 重開 ngrok 後，跑這支腳本更新 `ngrok-url.json` |
| `/opt/homebrew/etc/mosquitto/mosquitto.conf` | Mosquitto 設定（1883 + 9001 兩個 listener） |
| `/tmp/mosquitto.log` | Broker 詳細連線紀錄（除錯神器，見下方） |

---

## 二、Mosquitto / 網路層踩過的坑

### 1. `mosquitto.conf` 要同時開兩個 listener
```conf
listener 1883
protocol mqtt

listener 9001
protocol websockets

allow_anonymous true
```
凱比走 1883（原生 MQTT），瀏覽器走 9001（WebSocket）。兩個都要開，缺一個會有一邊連不上。

### 2. 想除錯連線問題，一定要開詳細 log
預設 Mosquitto 幾乎不留紀錄。加這幾行進 `mosquitto.conf`：
```conf
log_dest file /tmp/mosquitto.log
log_type all
connection_messages true
```
`brew services restart mosquitto` 之後，`tail -f /tmp/mosquitto.log` 可以即時看到誰連上了、訂閱了什麼 topic、訊息有沒有真的轉送出去——這比憑感覺猜「連了沒/收到沒」準確太多，後面好幾個坑都是靠這個 log 才抓到真正原因。

### 3. ngrok 免費帳號的限制
- `ngrok http <port>`（給 WebSocket 用）：免費帳號就能用
- `ngrok tcp <port>`（給原生 MQTT TCP 用）：**免費帳號需要先加信用卡驗證**（不會真的扣款，但要不要加卡是使用者自己的決定，工具沒辦法代為輸入卡號）
- 免登入的替代方案（serveo.net、localhost.run）實測都連不上 TCP 轉發，不穩定，不建議依賴

### 4. 最後採用「區網直連」取代 ngrok
凱比（透過 Roflow）跟瀏覽器（quiz.html）如果跟這台 Mac 在**同一個 Wi-Fi**，可以直接用 Mac 的區網 IP，完全不用 ngrok：
- 凱比／Roflow 連 `192.168.x.x:1883`（原生 MQTT，不加密）
- 瀏覽器連 `ws://192.168.x.x:9001`

**限制**：三者（凱比、開網頁的裝置、這台 Mac）必須同一個 Wi-Fi，離開這個網路就斷線。比賽現場如果換場地/網路，記得要重新確認 IP、或改回 ngrok。

### 5. ngrok wss:// 對 Roflow 不可靠
瀏覽器（mqtt.js）用 ngrok 的 `wss://` 完全沒問題，但 Roflow 那端的 MQTT 連線用同一組 `wss://` 網址**測試多次都連不上**（log 完全沒出現連線紀錄，代表連線在到達 broker 之前就失敗了），换成區網 IP 之後才穩定連上。原因未完全確認，懷疑跟 Roflow 的 MQTT 客戶端實作或 ngrok 免費版限制有關。**結論：Roflow 這端一律用區網 IP，不要用 ngrok。**

---

## 三、NUWA Roflow 平台的坑（最花時間的部分）

### 1. Roflow ≠ .koding，不要搞混
一開始以為要用「.koding」積木編輯器才能做觸摸偵測，繞了一圈才發現 **Roflow（服務流程）本身就有「觸碰分流」跟「MQTT」模組**，直接在 Roflow 裡做就好，不需要碰 .koding。

### 2. MQTT 伺服器設定：位址不要加協定前綴
新增 MQTT 伺服器時，「伺服器位址」欄位**只填主機名稱**（例如 `192.168.0.62`），不要加 `ws://`／`wss://` 開頭。加密用旁邊的「加密 (TLS)」勾選框控制，port 另外填。

### 3. Topic 前綴陷阱
如果「發送位置」選內建的 **Nuwa Server-01**，Topic 欄位前面會被強制加上一段裝置 ID（例如 `505832b1/`），實際發布的 topic 會變成 `505832b1/kebbi/touch`，跟網頁訂閱的 `kebbi/touch` 對不上。**换成自訂的伺服器（不是 Nuwa Server-01）之後，這個前綴就不會出現**，Topic 打什麼就是什麼。

### 4. 「一個節點只能接一條線出去」
除了「分流」類型的模組（語音分流、觸碰分流），其他一般節點（觸發條件、APP與網頁、機器人表現…）**都只能有一條輸出線**，沒辦法從同一個節點同時接到兩個不同的下一步。想要「同時做兩件事」，必須透過分流模組的多個分支，或是各自獨立的節點。

### 5. 「時間」節點的時段互斥
想建立第二條「隨時都成立」的獨立觸發鏈路，試過新增一個「時間」節點——結果系統**不允許時段重疊**（跳出「與已設定日期的時段，發生重疊」的錯誤），代表沒辦法有兩個「00:00-23:59 每天」同時存在。這條路走不通。

### 6. 語音分流的分支是「語音辨識結果」，不是通用的並行插槽 ⚠️ 最關鍵的坑
語音分流底下的分支，命名其實是「分流答覆 #1」「分流答覆 #2」「非預期答覆」「無人回應」——這些是**語音互動的結果分類**，不是隨便給你掛東西的通用分支。

實測發現：這個平台的「服務流程」本質上是**單一執行緒的對話狀態機**，不是真正能同時跑多條的並行系統。一旦語音分流選定某個分支（例如進入 touch 或某個 MQTT 監聽），流程就會「卡在」那個分支自己的迴圈裡（如果該分支有自己迴圈接回自己的話），不會再回頭考慮語音分流的其他分支。這代表：

- 把「接收 kebbi/action」掛在語音分流的「無人回應」分支下 → 只有語音分流判定「安靜沒人講話」時才會進入監聽，遊戲進行中很少會進到這個狀態，導致凱比大部分時間根本沒在監聽，訊息送到 broker 就直接消失（MQTT QoS 0 沒人訂閱就是真的不見，不會保留）
- 這是為什麼手動測試（充分等待時間）都成功，但實際自動遊戲流程卻沒反應的根本原因

**最終解法**：放棄追求「兩條完全並行、互不相關」的理想架構，改成把「接收 clap/wrong」**直接接在 touch 自己送出訊號的鏈路後面**（送出 head → 接收 clap/wrong → 機器人表現 → 迴圈接回 touch），變成同一條鏈路裡的序列步驟，而不是另開一條獨立流程。

### 7. MQTT 接收節點的迴圈要接回自己
「機器人表現」執行完之後的「連結至」，要指回**這個 MQTT 接收節點自己**，不能接到別的模組（例如 touch）。接錯的話，這個接收節點只會生效一次，之後就再也不會被監聽到。

### 8. 接收節點的「文字消息」比對欄位其實是正則表達式
選「文字消息」之後跳出來的輸入框寫著「輸入正則表達式」——雖然介面沒有特別強調，但這個欄位是 regex 比對，不是單純的完全相同比對。填純文字（例如 `clap`）當作簡單 pattern 是可以動的，不用跳脫任何字元。

### 9. 「未收到消息」的等待時間，實際影響比想像中小
一開始以為「最長等待時間」（例如 15 秒）代表**每次**都要等這麼久才會繼續，因此拼命想把它調短。後來確認：**只有真的完全沒收到任何符合的訊息時，才會等到這個時間上限才觸發逾時分支**；如果訊息在期限內到達，會立刻往下走，不會傻等到時限。所以這個數字比較像是「最壞情況的容忍上限」，不用太糾結要多短。

### 10. 送出跟接收之間有真實的競爭風險（race condition）
即使把接收節點正確接在送出後面，實測還是偶爾「答對了但凱比沒拍手」。原因：quiz.html 判斷完幾乎瞬間（< 1 秒）就發送 `kebbi/action`，但凱比從「剛送出 touch 訊號」切換到「開始監聽 kebbi/action 這個節點」需要一點處理時間（連線到 NUWA 雲端、重新訂閱）。如果網頁發送得比凱比準備好監聽還快，這則訊息就直接消失，沒有補救機制。

**解法**：在 `quiz.html` 裡故意把 `publishAction()` 延遲一小段時間才真的發送（目前設定 1.51 秒，可依實測調整），留時間給凱比完成節點切換。程式碼位置：`quiz.html` 第 424 行 `ACTION_PUBLISH_DELAY_MS`。

### 11. MQTT 接收節點可以把訊息內容存成變數
節點下方「變數」區塊的「設定」，只有在「執行動作」選「**接收訊息**」時才會出現「MQTT_接收的訊息」這個選項（選「發送訊息」時該區塊只有跟送出相關的變數，例如 `MQTT_發送的訊息`、`MQTT_Topic`）。抓到之後可以綁定成自訂變數名稱，之後用 `{{變數名稱}}` 語法插入到 TTS 或其他欄位裡引用——例如讓凱比把收到的內容講出來，或拿去做條件分流。目前正式版沒有用到這個功能（用兩個獨立 Topic 分別比對 clap/wrong 就夠用了），但這是後續想精簡架構、或想做更複雜反應時的路。

### 12. 「APP與網頁」節點設「不自動關閉」會卡住整個流程
如果用這個節點在凱比螢幕上開網頁，設定「不自動關閉」會讓流程**停在這一步，不會往下走**，導致後面接的所有節點都執行不到。改成「顯示 X 秒後自動關閉」雖然能繼續往下走，但這樣網頁畫面只會閃一下就消失，不適合當作「持續顯示答題畫面」的做法。

**最終決定**：不讓 Roflow 負責開網頁。改成人工在比賽現場另外找一台螢幕/平板，手動開好 `quiz.html` 放著，凱比純粹當觸控感測器用，不管畫面顯示。

---

## 四、quiz.html（網頁端）踩過的坑

### 1. Safari 的 Web Audio 振盪器不可靠
一開始用 `AudioContext` + `createOscillator()` 即時合成答對/答錯音效，Chrome 上沒問題，**Safari 上卻常常沒聲音**，即使照標準做法在 resume() 完成後才播放也一樣。同時間語音合成（`speechSynthesis`）卻完全正常，證明不是「自動播放被擋」這種通用問題，而是振盪器這條播放路徑本身在 Safari 上不穩定。

**解法**：放棄即時合成，改用 Node.js 預先合成短音效（幾個 sine/sawtooth 音階組成的 WAV），轉成 base64 data URI 直接內嵌在 `quiz.html` 裡，用標準 `<audio>` 元素播放（跟語音合成走同一套播放機制，比較不會被擋）。

### 2. 手機/平板要跟 Mac 同一個 Wi-Fi 才連得到本機伺服器
`quiz.html` 的檔案本身是用 `python3 -m http.server 8642` 從這台 Mac 本機發布的，只有同一區網內的裝置能訪問 `http://192.168.x.x:8642/quiz.html`。正式上場前應該部署到 GitHub Pages，才有不受區網限制的穩定網址。

---

## 五、還沒做完 / 待確認事項

- [ ] 正式部署到 GitHub Pages（目前還是本機 `http.server`，重開機或關掉終端機視窗就會失效）
- [ ] 比賽現場如果換 Wi-Fi/場地，要重新確認凱比、顯示螢幕、Mac 三者的區網 IP，或改回 ngrok 並更新 Roflow 裡的伺服器位址
- [ ] `ACTION_PUBLISH_DELAY_MS`（目前 1.51 秒）建議正式上場前再用真實網路環境多測幾次，微調到最穩定的數值
- [ ] 進階模式（先摸頭、再摸右手）的複合指令，程式碼已完成但還沒在真實環境跑過
